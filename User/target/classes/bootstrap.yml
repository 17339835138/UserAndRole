spring:
  cloud:
    config:
      name: user-user
      profile: dev
      discovery:
        enabled: true
        service-id: config-demo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/